server:
  port: 8080
  error:
    include-stacktrace: never
    include-message: always

spring:
  mvc.pathmatch.matching-strategy: ant_path_matcher
  neo4j:
    uri:
    authentication:
      username:
      password: